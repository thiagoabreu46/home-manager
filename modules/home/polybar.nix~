{ pkgs, config, ... }:

{    
   services.polybar = {
     enable = true;
     config = {
     
  "bar/top" = {
    monitor = "\${env:MONITOR:DP-2}";
    width = "100%";
    height = "3%";
    radius = 0;
    modules-center = "date";
  };

  "module/date" = {
    type = "internal/date";
    internal = 5;
    date = "%d.%m.%y";
    time = "%H:%M";
    label = "%time%  %date%";
  };
};
   extraConfig = ''

  [colors]
   black = #000
   white = #fff 
   blue = #00f
   green = #0f0

  
  '';
};
  services.polybar.script = ''
  polybar bar &
  '';
}